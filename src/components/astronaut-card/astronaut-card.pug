mixin astronaut-card(name, image, desc, phone)
  article.grid-card(tabindex='1')
    .card
      .shine

      img(src=image alt=name)

      h3.name= name

      .info
        p= desc
        p= phone
